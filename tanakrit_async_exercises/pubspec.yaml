name: tanakrit_async_exercises
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.8

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  flutter_dotenv: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
